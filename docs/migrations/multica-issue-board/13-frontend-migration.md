# 13 — Formal frontend migration

## Goal

Migrate the reference frontend from `cloud前端/` into `cloud/frontend/` as Ora Cloud's **official
web frontend** — move/reuse/adapt, not rewrite. It must belong to `cloud`, install/build/run on its
own, and talk to the real backend over the HTTP API; no symlinks and no cross-directory imports into
`../cloud前端`.

## What the frontend is

React 19 + TypeScript + Vite + Tailwind CSS 4 + TanStack Query. The API layer (`src/api/`) is
generated by orval from `api/openapi.json`, so the frontend and backend share one contract. Auth
reuses Cloud's dual-JWT: the browser holds only a session cookie + user profile; `cmd/ora-web`
signs tokens server-side (the browser is untrusted). Dev proxy forwards `/api`, `/internal`,
`/healthz` to `:8080`.

## Issue features implemented against the real backend

- Board (Kanban): columns, cards, drag-to-move via fractional `position` (`/issues` + `/move`).
- Issue CRUD, status, priority, title, description, assignee, parent/sub-issue, labels, comments,
  subscribers, search (`?q=`), batch update, saved views, grouped views, `properties`.
- Wave 3A surfaces: `assigneeType`/`assigneeId`/`projectRef` on issues; comment `parentId` /
  `authorType` / `authorId` / `seq`; IssueRun create/list/get; IssueContextRef list/create/delete.
- New code prefers `assigneeType`/`assigneeId` over the legacy `assigneeUserId`.

## Deliberately unavailable (shell / "Not connected" / "Coming later")

These have no backend capability yet, so the frontend shows a disabled or unavailable state instead
of faking production behavior:

- Assignee of type `agent` / `team` — no resolver; UI marks them unavailable.
- Project details — `projectRef` is an opaque UUID; the UI shows `projectRef` and
  "Project details unavailable".
- Runs — the detail page renders the runs **list only**; the run control is a **disabled placeholder**
  ("运行（暂不可用）"). `useCreateRun` exists in the API layer but has **no caller**, so the UI has no
  create-run path at all. (An earlier revision of this doc claimed the UI shows "Run created / queued"
  — that was inaccurate and is corrected here.)
- Context refs — list/add/remove only (no smart hydration of referenced objects).
- Execution, realtime/WebSocket, PR integration, execution logs, full Timeline — placeholders.

## Collaboration-UI starting point (recorded for Wave 3B-1; delivered by Wave 3B-1)

Measured from the migrated code, so the next wave does not re-discover it:

| Area | Actual state |
| --- | --- |
| Comment composer | Wave 3B-1: posts `{ body, targets[] }` — `@` target picker (`GET /collaboration/targets`), Mention Mode (no task), Agent/Team Task Mode (task required), Workflow shown as "本阶段不可用". |
| Comment threading | `CommentItem` renders `parentId` indentation, but the composer **still never sends `parentId`** — threads cannot be created from the UI (remains open). |
| Timeline | Wave 3B-1: Activity panel reads `GET /issues/{iid}/timeline` (Comment + Activity by shared `seq`) instead of comments + runs separately. |
| Search | `useIssues(tid, q?)` accepts `q`, but **no caller passes it**; there is no search input. |
| Pagination | None — `listPage()` unwraps `data.items` and never sends `limit`/`after`. |
| Generated client | orval output exists but **nothing imports it**, and it has **no `issues` tag**; all Issue features use hand-written hooks over `AXIOS_INSTANCE`. |
| Runs | List only; create not wired (see above). |
| agent / squad / runtime / skill / chat / inbox / billing / project pages | Served by **MSW mocks** (`/mock-api`), not the real API. |

Wave 3B-1 delivered the `@` picker and Mention/Task Mode on top of the real API
([12-collaboration-architecture.md §37.14](12-collaboration-architecture.md#3714-frontend-interaction-model)).

## Workflow UI (built by Wave 3B-2)

The fourth `InteractionDescriptor` mode (`form`) is **implemented**
([§38.27](12-collaboration-architecture.md#3827-frontend-component-architecture-frozen)). The
component boundary that shipped:

```
TargetPicker → WorkflowInteractionComposer
                 ├── DynamicFormRenderer  ← renders from FormDescriptor ONLY
                 │      └── FormFieldRenderer
                 ├── AssistSuggestions    ← current vs suggested, Apply / Ignore
                 ├── ContextSelector      ← REUSE the existing issue context-refs UI
                 └── ConfirmReview        ← target + values + context → Back / Confirm
```

Hard rule, honoured: `DynamicFormRenderer` renders **only** what the descriptor declares — a branch
like `if (workflow.id === "security-review")` exists nowhere.

**Composer model (revised after product review).** The activity column reads top-to-bottom the way a
person works — history first, then the place where new work is created:

```
活动
├── 交互历史              Comment + Activity 按 seq
├── 已确认的 Workflow     「已确认 · 运行 <status>」，不再提供第二次配置
├── 添加评论              Textarea + [评论]        ← 纯评论
└── @提及 / 任务
    ├── [提及 / 任务 ▾]   选择目标（只暂存，不发请求）
    ├── [头像] Alice        [留言…            ] [提交] [×]
    ├── [头像] Backend Agent[这个目标要做什么… ] [提交] [×]
    └── [头像] Security Review Workflow
          ┌ ⌄  ⚙ Security Review  [Form Mode]  × ┐   ← 折叠在左，取消在右
          │  (descriptor 驱动的动态表单)           │
          │  [✨ AI Assist]  [审阅]                │
          └───────────────────────────────────────┘
```

Everything below the history is a **draft**: selecting a target stages it locally, each target has its
own message box and its own submit button, and the workflow panel is written to the server only when
「确认执行」 is pressed (comment → interaction → run). An abandoned draft leaves no trace in the
Timeline. `PendingTargets` owns the staged rows, `TargetPicker` only picks, and
`WorkflowInteractionComposer` is the draft form.

## Not in scope this pass (do not build)

Wave 3B ports, the (superseded) 0008 simulator catalog, fake Agent/Team/Workflow/Executor backends, real agent
execution, workflow backend, realtime, PR backend, execution-logs backend, notification system,
LLM/runtime.

## Demo vs formal

- `cmd/demo-issue-board-web/` — single-file HTML Kanban demo, no frontend dependency, server-side
  JWT signing. Kept for manual HTTP verification of the Issue API.
- `frontend/` — the formal product frontend, replacing the reference `cloud前端/`.

The two coexist; the demo is not the product UI.

## Verification

- Backend: `go build ./...` and `go test ./internal/... ./cmd/...` PASS.
- Frontend: `npm ci` / `npm run dev` / `npm run build` / `npm run check` (lint, typecheck, tests)
  are required and need Node >= 24 (`.node-version` + `engine-strict`). Wave 3B-1's pre-freeze pass
  (2026-09-20) ran them under **Node 24.21.0** with a clean `npm ci`: `typecheck`, `test`
  (28 files / 76 tests), `build`, `check:modules`, `check:docs`, `check:dead` and `check:dup` all
  PASS. `lint` and `format:check` are still red, but every violation is **pre-existing debt** in
  files Wave 3B-1 did not author (confirmed by re-running both against the `HEAD` revisions);
  this wave adds no new violations.
